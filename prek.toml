# Workspace root for prek — https://github.com/j178/prek
#
# There are no repo-wide hooks: each project carries its own config and prek
# runs it from that project's directory. See musiclib/prek.toml.
repos = []
