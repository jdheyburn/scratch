"""musiclib — music library tooling.

`musiclib vinyl sync` takes a record from Audacity export to a verified beets
import. See the design doc for why each step is shaped the way it is.
"""

__version__ = "0.1.0"
